export enum ELEMENT_TYPE {
  GROUP = 'group',
  TEXT = 'text',
}

export enum TARGET_TYPE {
  COMPONENT = 'component',
  PAGE = 'page',
}

export enum PREVIEW_PARAM {
  TARGET = 'target',
  TYPE = 'type',
}

export enum IFRAME_EVENT {
  CHANGE = 'change',
  INIT = 'init',
  READY = 'ready',
}
